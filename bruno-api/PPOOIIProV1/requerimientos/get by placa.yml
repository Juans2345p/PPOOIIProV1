info:
  name: get by placa
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/vehiculos/placa/AAA000
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
